# DeepSeek OCR + PageIndex System + DocuFlow Library Services
# Minimal requirements - vLLM and cuda packages should be installed separately via uv

# Core OCR & API
openai>=1.0.0           # OpenAI client for vLLM
httpx>=0.27.0           # HTTP client
fastapi>=0.100.0        # API framework
uvicorn>=0.20.0         # ASGI server

# Document Processing

PyMuPDF>=1.26.0         # PDF rendering
PyPDF2>=3.0.0           # PDF reading
pdfplumber>=0.10.0      # PDF table extraction (legacy; replaced by docling-parse)
Pillow>=10.0.0          # Image processing
python-docx>=1.1.0      # DOCX file support
docling-parse>=6.0.0    # PDF text extraction (primary, replaces PyMuPDF for text pages)


# Database
SQLAlchemy>=2.0.0       # ORM

# Configuration
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# PageIndex Dependencies
tiktoken>=0.5.0         # Token counting
PyYAML>=6.0.0           # Config files

# Authentication
python-jose[cryptography]>=3.3.0   # JWT tokens
passlib[bcrypt]>=1.7.4             # Password hashing
bcrypt<5.0                         # Pin bcrypt 4.x for passlib compat
python-multipart>=0.0.6            # Form data / file uploads

# Language Detection
langdetect>=1.0.9       # Auto-detect source language

# Keyword Extraction
scikit-learn>=1.3.0     # TF-IDF for keyword extraction

# Optional: Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
