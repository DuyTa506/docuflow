# DeepSeek OCR + PageIndex System + DocuFlow Library Services
# Minimal requirements - vLLM and cuda packages should be installed separately via uv

# Core OCR & API
openai>=1.0.0           # OpenAI client for vLLM
httpx>=0.27.0           # HTTP client
fastapi>=0.100.0        # API framework
uvicorn>=0.20.0         # ASGI server
temporalio>=1.7.0       # Durable digest pipeline workflows

# Document Processing

PyMuPDF>=1.26.0         # PDF rendering + overlay translation
fonttools>=4.0.0        # PyMuPDF subset_fonts for overlay output
PyPDF2>=3.0.0           # PDF reading
pdfminer.six>=20221105  # PDF text-layer glyph extraction (pdf overlay)
pdfplumber>=0.10.0      # PDF table extraction (legacy; replaced by docling-parse)
onnxruntime>=1.17.0     # DocLayout ONNX model for formula zone detection
onnx>=1.15.0            # ONNX model metadata
numpy>=1.24.0           # Layout grid for pdf overlay
opencv-python-headless>=4.8.0  # DocLayout image preprocessing
tenacity>=8.2.0         # Retry for overlay translation chunks
babeldoc>=0.1.0         # DocLayout ONNX weights + CJK fonts (optional at runtime)
Pillow>=10.0.0          # Image processing
python-docx>=1.1.0      # DOCX file support
docxtpl>=0.16.0         # Jinja2 DOCX template rendering (digest export)
docling-slim[convert-core,format-pdf,parse]>=2.36.0  # layout + tables + reading order
docling-parse>=7.0.0    # page classification (text vs scanned)
docling-ibm-models>=3.0.0
transformers>=4.40.0
# PyTorch GPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130


# SQLAlchemy>=2.0.0       # ORM
SQLAlchemy>=2.0.0       # ORM
psycopg2-binary>=2.9.9  # PostgreSQL driver
alembic>=1.13.0         # Schema migrations

# Object storage (MinIO / S3-compatible)
minio>=7.2.0

# Configuration
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# PageIndex Dependencies
tiktoken>=0.5.0         # Token counting
PyYAML>=6.0.0           # Config files

# Authentication
python-jose[cryptography]>=3.3.0   # JWT tokens
passlib[bcrypt]>=1.7.4             # Password hashing
bcrypt<5.0                         # Pin bcrypt 4.x for passlib compat
python-multipart>=0.0.6            # Form data / file uploads

# Language Detection
langdetect>=1.0.9       # Auto-detect source language

# Keyword Extraction
scikit-learn>=1.3.0     # TF-IDF for keyword extraction

# Optional: Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
